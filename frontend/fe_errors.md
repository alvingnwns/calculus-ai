- 2026-03-20: Node string interpolation issue destroyed template literals during `page.tsx` update, resulting in `Parsing ecmascript source code failed` (for example `${API_BASE_URL}/calculate` became malformed). Fixed by directly repairing string templates in `frontend/app/page.tsx`.
- 2026-03-20: Next.js production build failed on strict type checking for `react-katex` and `react-plotly.js` (`implicitly has an 'any' type`). Fixed by installing `@types/react-katex` and `@types/react-plotly.js`.
- 2026-03-20: Left panel width class was accidentally set to invalid `lg:w-400` instead of `lg:w-[400px]`, causing desktop layout collapse. Fixed by setting `lg:w-[400px] shrink-0`.
- 2026-03-20: `Recent Computations` became non-interactive after UI redesign (display-only cards). Fixed by making cards clickable and loading saved result/explanation into the output panel.
- 2026-03-20: Result notation showed literal `cdot` text and caret-style powers due to over-escaped multiplication formatting in frontend LaTeX conversion. Fixed by normalizing multiplication formatting (implicit `2x` where simple) and correcting LaTeX escaping/exponent grouping.
- 2026-03-20: Phase 3 chat panel integration completed with no new blocking frontend build/type errors.
- 2026-03-20: Assistant chat responses rendered math as raw text (e.g., `$\\sin(x)$`) because chat bubbles used plain text rendering instead of markdown+math rendering. Fixed by rendering assistant messages with `ReactMarkdown`, `remark-math`, and `rehype-katex`.
- 2026-03-20: `page.tsx` bloat concern addressed via hook extraction; no new blocking compile/type issues after reducing file size.
